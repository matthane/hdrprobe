# Rust build artifacts
/target

# Test corpus: real movie content is large and copyrighted — never commit it.
# The whole testfiles/ tree is local-only and untracked.
/testfiles/

# Tool logs / scratch
*.log
