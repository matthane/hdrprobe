# Third-Party Licenses

hdrprobe is licensed under the MIT License (see [LICENSE](LICENSE)).

This file is generated by `cargo about generate` from `about.toml` and `about.hbs` —
**do not edit it by hand.** It lists every third-party Rust crate compiled into the
`hdrprobe` release binary, grouped by license, with each license's full text (which carries
the crates' required copyright notices). Every license here is permissive and compatible
with hdrprobe's MIT license. Include this file when redistributing compiled binaries.

To refresh it after a dependency change: `cargo about generate about.hbs -o THIRD-PARTY-LICENSES.md`.

## License summary

| License | Crates |
|---|---|
{{#each overview}}| {{name}} | {{count}} |
{{/each}}

{{#each licenses}}
## {{name}}

Used by:

{{#each used_by}}
- **{{crate.name}} {{crate.version}}**{{#if crate.repository}} — <{{crate.repository}}>{{/if}}
{{/each}}

```
{{{text}}}
```

{{/each}}
